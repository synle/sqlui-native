require("./common");

log(`
=========================================================
# postbuild.js
# No-op: electron-builder uses directories.app = "build"
# so all built assets are already in the right place.
=========================================================
`);
