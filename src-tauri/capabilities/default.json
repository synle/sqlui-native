{
  "$schema": "https://raw.githubusercontent.com/nicedoc/tauri/dev/.schema/capabilities.schema.json",
  "identifier": "default",
  "description": "Default capability set for the main window",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open", "dialog:default", "opener:default"]
}
