// import YOUR_ADAPTER_NAME from 'src/common/adapters/YOUR_ADAPTER_NAME/index';

// const adapter = new YOUR_ADAPTER_NAME('some_connection');

describe.skip("YOUR_ADAPTER_NAME", () => {
  test("Sample test", async () => {
    expect(1).toBe(1);
  });
});
